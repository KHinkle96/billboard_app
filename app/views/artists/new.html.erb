<h1>Add Artist</h1>
<%= form_with(model: @artist, local: true, class: "ui form") do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, required: true, autofocus: true, placeholder: "First and Last Name" %>
  </div>
  <%= f.submit nil, class: "ui green button inverted" %>
<% end %>