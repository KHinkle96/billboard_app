<% songs.each do |song| %>
    <div class="song-card">
      <p><%= song.title %></p>
      <p><%= song.genre %></p>
      <p><%= song.released_on %></p>
      <p><%= song.rating %></p>
    </div>
  <% end %>